# AI Content Machine access gate — copy to .env.local (gitignored) and on Vercel
# the password visitors need to open the live app
AICM_ACCESS_PASSWORD=
# Resend API key (resend.com) — access requests are emailed with it
RESEND_API_KEY=
# where access requests go (your inbox)
AICM_REQUESTS_TO=
