import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Visual Direction · Anand",
};

export default function VisualDirectionLayout({ children }: { children: React.ReactNode }) {
  return children;
}
