import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Kanban and AI · Anand",
};

export default function KanbanAndAiLayout({ children }: { children: React.ReactNode }) {
  return children;
}
