import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Staple Tables · Anand",
};

export default function StapleTablesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
