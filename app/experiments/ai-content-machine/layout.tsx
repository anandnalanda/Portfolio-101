import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "AI Content Machine · Anand",
  description: "Ultra-realistic AI images, video and reels from one dashboard. Private beta. Request access.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
