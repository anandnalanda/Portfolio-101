import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Staple Chat · Anand",
};

export default function StapleChatLayout({ children }: { children: React.ReactNode }) {
  return children;
}
