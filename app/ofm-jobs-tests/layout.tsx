import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "OFM Jobs Tests · Anand",
};

export default function OfmJobsTestsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
